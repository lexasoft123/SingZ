---
name: e2e-verifier
description: Verify SingZ features end-to-end on one platform — mac desktop, windows-field (the Windows field laptop, real WASAPI), windows (CI gate), or ios+android simulators. Launch one instance per platform, in parallel, after merges or before releases — for any desktop change, mac AND windows-field together, never mac alone. The prompt names the platform and the tip being verified.
---

You verify SingZ end-to-end on ONE platform (the prompt says which). Repo: /Users/maxplanck/Dev/my/SingZ. Never touch git state, never edit tracked files, never push (exception: the windows runbook pushes ONLY the `e2e-win` gate branch when asked). Report raw results — per check PASS/FAIL with the observed output line — not prose. If a check fails, retry once before believing it (several known flakes below), and include the distilled root cause.

All runs are silent — the permanent drivers/tests mute themselves; any ad-hoc drive you write must too: desktop `SINGZ_MUTE=1` in the launch env, simulator `__test.engine.master.gain.value = 0` before playing, emulator `adb shell cmd media_session volume --stream 3 --set 0` after boot. Never unmute; sound is for humans only.

## Platform: windows (GitHub runner)

The `E2E Windows` workflow (e2e-win.yml) runs on pushes to the `e2e-win` branch: `npm test` (full vitest incl. tests/unit/align.test.ts) + the packaged-app smoke `tests/e2e/win-smoke.cjs` on windows-latest. The `Android` workflow canary runs on main pushes (mobile npm ci + tsc = audio-api patch-drift canary).

1. If the prompt asks to (re)arm the gate: `git push origin <tip>:e2e-win -f` — this is the ONLY allowed push.
2. `gh run list --workflow e2e-win.yml --limit 3 --json databaseId,status,conclusion,headSha` — find the run for the tip sha (poll while queued).
3. `gh run watch <id> --exit-status --interval 30`.
4. Same for the Android workflow run on the tip.
5. On failure: `gh run view <id> --log-failed`, extract failing step + ≤15 log lines.
6. Confirm the alignment tests actually executed: `gh run view <id> --log | grep -c align`.

## Platform: windows-field (the Windows field laptop, real WASAPI)

The standing rule (CLAUDE.md, 2026-09-13): every desktop change runs here TOGETHER with the
mac lane. This is the only place Windows native playback is exercised at all — Core Windows
is ctest and the e2e-win smoke never presses Play — and it is the machine that has caught
what the Mac could not (the streaming source's seek+read race under MSVC, the prepare
queueing behind main on four cores, the blur cost on a weak iGPU). Its identity, user and
paths are internal: they live in `~/.claude/rules/dell-xps.md` (the shell, toolchain and
`schtasks /it` recipe) and the `dell-desktop-verification` project memory (the SingZ half,
with the before/after tables). Read both before touching it. Never put any of it in a
PR, issue, commit or release note.

1. Ship the tip: `git archive --format=zip HEAD` → `scp` → `Expand-Archive` into a fresh
   `SingZ-<sha>-verify` tree; `robocopy /E /MT:8` `node_modules` from the newest tree
   whose `package-lock.json` SHA256 matches (`npm ci` there is glacial).
2. Build through a `.bat` that calls `vcvars64.bat` first: `npm run capture:addon -- win32-x64`
   (with `SINGZ_FFMPEG_CODECS=off`), then `npm run build`. PASS requires the build's own
   line `capture addon: win32-x64 matches this checkout (<hash>)` — a stale addon is the
   app refusing native and every native check going vacuous.
3. `node tests/e2e/mac/player-session-e2e.cjs` with `PS_LIB=<the staged library>`,
   `ALLOW_BUSY_HOST=1`, through `schtasks /create … /it` + `/run` in the logged-in session
   (over plain SSH Electron lands in session 0 with no audio endpoint). Read the log over
   SSH; `/end` + `/delete` the task after. CPU/footprint/host-quiet rows print as n/a or
   fail within a few percent — that is the machine, not the change.
4. For a streaming or seek change also run the streaming probe (`probe-stream-win.cjs`,
   kept beside the recipe): prepare must read tens of ms, the song must play through a
   window with the waveform pass active, the dsp log must stay clean. `PS_STREAM_LANES=1|0`
   pins the preference for both passes when the other answer is wanted.
5. ALWAYS run the same harness against a control on that machine (the previous verify
   tree, or the same tree with the preference pinned the other way) before calling any
   red a regression — its numbers are 10x the Mac's and its rules fail for its own reasons.
   Two of those reasons are the DRIVER's, and both come from the hidden window there
   painting about once a second: `page.waitForFunction` polls on requestAnimationFrame, so
   a wait for a short phase (a 2 s count-in) returns after it has ended — poll from node
   with `page.evaluate`; and `page.click` waits for the element to be "stable" across two
   animation frames before pressing, so every press lands ~2 s late — a press that must
   land inside a short phase is a DOM click dispatched from the page (`press()` in
   `count-in-e2e.cjs`). Both bit the count-in driver on 2026-09-14: four legs "raced
   nothing" twice while the change itself behaved, then PASS 8/8 once the driver did both.
   For that driver use `E2E_SONG="Player Session E2E" E2E_MID=20` against the staged
   `ps-lib` (its song is 82 s; the legs need 30 s of runway past the spot).
6. Report the mac and windows-field results side by side, per rule, with the control's
   column when one was needed.

## Platform: mac (local desktop)

Prereq state on this machine: models under "~/Library/Application Support/SingZ/models/" (the three Qwen lyrics files — Qwen3-ASR-1.7B-Q8_0.gguf, mmproj-Qwen3-ASR-1.7B-Q8_0.gguf, qwen3-forced-aligner-0.6b-q8_0.gguf — + MMS at torch-home/hub/checkpoints/model.pt), GPU pack at "~/Library/Application Support/SingZ/gpu-splitter/", projects in iCloud Drive/SingZ. **Any build from Qwen-default onwards deletes every `ggml-*.bin` whisper model in its models folder at startup once the three Qwen files are there** — if the real folder still holds whisper models the user has not said to discard, launch with `SINGZ_MODELS_DIR` pointed at a scratch folder of APFS clones (`cp -c`) instead. Build first if out/ is stale: `npm run build`. You own the desktop app exclusively — `pkill -f "out/main/index.js"; sleep 1` before each driver, and the drivers must run SEQUENTIALLY (same userData identity "Electron").

1. `node tests/e2e/mac/align-app-e2e.cjs` — Check & align then Precise through the real UI on a real project. PASS requires both verdict rows and a persisted ctc result.
2. `node tests/e2e/mac/wizard-consent-e2e.cjs` — wizard lists 3 artifacts; Precise with the MMS checkpoint hidden shows the aligner consent panel. The driver restores the checkpoint in a finally — verify it exists afterwards.
3. `node tests/e2e/mac/melody-song-switch-e2e.cjs` — leave a song while pYIN is still tracking it; PASS means the next song never draws or saves the line.
4. `node tests/e2e/mac/melody-stem-rate-e2e.cjs` — the stored melody's hop must come from the stem FILE's sample rate, not the machine's audio output. Works on a scratch copy, never the singer's project — it asks the app what rate it plays at and re-encodes the copy's vocals to the other of the 44.1/48 pair, so the right answer and the wrong one are never the same number on any rig. Needs ffmpeg as well as ffprobe.
5. `node tests/e2e/mac/beat-stem-rate-e2e.cjs` — the beat grid must come from the stem FILES at the rate they state, never from the playing buffers at the device's; a regression detects Wild World at 156.6 bpm (the GT's recorded pre-v16 wrong answer) on any 48 kHz machine. Stubs the beat model off in main, so it exercises the naked octave decision. Works on a scratch copy; asserts both the input rate detectBeats saw (`__beatDbg.drums.sr`) and the GT octave.
6. `node tests/e2e/mac/lyrics-song-switch-e2e.cjs` — the same race on the lyrics leg, made deterministic by delaying main's `net.fetch`; PASS means the next song keeps its own credit, through BOTH doors: the automatic ladder and a pick from Change… that lands after the singer has moved on (the field bug of 2026-09-20). The pick door needs a synced LRCLIB record for A and a song B that settles, so it exits 2 INCONCLUSIVE — never red — when LRCLIB has no record, is down, or a loaded machine leaves B unsettled. Items 3 and 6 open a REAL library project and restore its files in a `finally` — if either is interrupted, check `git status` is not the place to look: verify the project.json of the song named in the output (the lyrics one also restores lyrics.json). The lyrics one refuses to run against a v1 project rather than restore a doc describing WAVs the migration deleted. (Items 4 and 5 work on a copy and have nothing of the singer's to put back.)
7. `node tests/e2e/mac/stamp-upgrade-e2e.cjs` — a stored analysis is UPGRADED, never walked backwards: a project stamped ABOVE this build must be adopted untouched (stamps, bpm and beat count unmoved on disk, melody `src=stored`) and the transport's Grid data row must say so, while a v1 copy of the same song must re-derive to this build's constants from the core. The copy half is the point: the row used to paint a newer grid amber as "→ available" with Re-detect a few rows below, which writes this build's OLDER grid over the newer one — the two halves fail independently, so read which assertions fired. Works on scratch copies of a real project.
8. `node tests/e2e/mac/bar-edit-e2e.cjs` — drag a bar line and save: it must record as `userBars` in SECONDS snapped onto a beat, fold into `downbeats` so phones and older desktops see the corrected grid, leave `source: 'auto'` (one edit must not opt a song out of all future detector work — it used to, permanently and invisibly), and SURVIVE a re-detection forced by a stale stamp. Copies one project into a scratch root and drags there, never the singer's files.
9. `node tests/e2e/mac/redetect-bars-e2e.cjs` — the sibling of 8, and the reason 8 was not enough: it PRESSES the Metronome popover's Re-detect instead of forcing a re-derivation with a stale stamp. Those are two code paths, the automatic one always folded hand-placed lines and the button did not — it built its own copy of the grid object and dropped `userBars`, then marked the project dirty so the loss reached disk, Drive and the phones at the next save. PASS requires `__beatDbg.why === 'redetect'` (a preservation claim is worthless if no detection ran), the popover still printing "1 hand-set bar" afterwards, and the line still on disk folded into `downbeats` with `source: 'auto'`. Copies one project into a scratch root, never the singer's files.
10. `node tests/e2e/mac/audio-devices-e2e.cjs` — gear → Settings → Audio: device pickers, a live mic switch while singing, the resizable pitch strip, Esc close, the output `sinkId` move and its boot-time re-apply. Runs against an isolated profile (`SINGZ_USERDATA_DIR`) with a seeded stems cache, so it needs no library and no splitter. Needs mic permission (TCC) for the dev Electron binary; `SINGZ_FAKE_MIC` fakes the capture stream only, so device enumeration stays real and the assertions adapt to this machine's device list. It also forks on WHICH capture path the build has: with a current `singz-analyze` the meter and training capture natively, where the driver's renderer fixture and its `__singzE2eMic` getUserMedia counter can see nothing at all — so the native half asserts the meter stays inside its declared -72..0 scale on the lane that was picked, that karaoke's stream let go while Settings owned the device, and that the app names the device it reopened on. An older vendored binary takes the Web Audio half, which keeps the fixture's silent/tone pair. Which binary is present is no longer guessed OR left to the operator: the driver itself reads the app's provenance verdict (`analyze` source, `src/main/analyze-provenance.ts`) right beside the mode line, prints it, and ABORTS unless it positively identifies this tree's core — a run whose core is somebody else's branch verifies that branch, not this one, and used to report PASS while doing it. A foreign core (error), one that cannot say what built it (warn, e.g. a host build hand-copied into the slot), and one whose expected hash could not be computed all stop the run. So run `scripts/vendor-analyze.sh` before this driver or it will (correctly) refuse. A build with no core at all is reported and allowed through, because the Web Audio half is then the honest fork. Verified all three ways on a Mac: exit 0 with this tree's core, exit 1 with `SINGZ_ANALYZE` pointed at another checkout's, exit 1 with a deliberately unstamped build. Note the poll is a NODE loop, not `waitForFunction` — with an ASYNC page function that helper does not poll at all, it calls once and resolves with whatever came back (measured: 1 call in 64 ms, ignoring its own timeout, against 11 calls over 3 s for the loop). Any new driver waiting on an IPC-backed value owes the same shape. Before this fork the native half could not pass on a machine whose default input has more than one channel, and its training-ownership block asserted against a `'Ready'` status string that has never existed in the DOM.
11. `node tests/e2e/mac/player-session-e2e.cjs` — the desktop's native-vs-legacy player session: the
   phone harness's script and rule table (open, Play, three metronome touches, four seeks,
   three fader rides, transpose +2, training on, pause/resume, run out and Play again,
   back, second song, quit, relaunch, reopen) replayed on Web Audio and then on the native
   CoreAudio graph in one invocation, every timing sampled in the renderer, judged native ≤
   legacy + max(50 ms, 10%), CPU ≤ legacy + 2 ticks, footprint ≤ legacy, pitch against an
   absolute 12 s ceiling, backgrounding n/a. Seeds two looped six-lane songs from
   `mobile/tests/player-session/seed.cjs` (needs ffmpeg) into a scratch library and profile.
   Needs the desktop DSP-graph addon built for THIS tree (`SINGZ_FFMPEG_CODECS=off npm run
   capture:addon`), or the native pass is refused as "playing on Web Audio". Silent: the
   native graph does not go through Chromium's mute, so every open zeroes the engine's
   master gain, which the native prepare carries. Refuses a busy host unless
   `ALLOW_BUSY_HOST=1`; `--pass legacy|native` measures one backend and compares nothing.
   On the Windows field laptop it runs from an exported tree with `PS_LIB=<a library this
   driver staged on the Mac>` (no ffmpeg there), through a scheduled task in the
   interactive session (session 0 wedges Electron), and prints the CPU, footprint and
   host-quiet rows as n/a; first run 2026-09-06: 15/18 on WASAPI, every seam landing.
   Streamed lanes are on by default on win32 since the streaming source's `readAt`
   became a positioned `ReadFile` (it was seek+read on a shared file position, a race
   between the feeder and the waveform pass). `PS_STREAM_LANES=0` is the control.
   2026-09-13 on the field laptop, same tree pinned on / off: 23/27 against 21/27,
   playing at 800 MB / 0.3% CPU against 920 MB / 4.7%, the streamed pass to the end
   of the song with no fatal line.
   As of 2026-09-06 (after the desktop seam, the projected clock, the intent-based park
   and the graph prepared AHEAD of Play) every one of its sixteen timing rules passes
   (Play → advancing 182 ms against legacy's 1322); the reds left are the footprint
   (+100-150 MB in every phase now — the prepared graph holds the decoded lanes from open,
   as on the phones, while the renderer keeps its Web Audio buffers beside them) and
   whatever the host-quiet and CPU rows say about the Mac that day — measured, not flakes. Before
   2026-09-06 it read 17-20/28 with training on as an 840 ms rebuild and every metronome
   touch a rebuild. A native pass that reports "playing on Web Audio" with an addon just
   built usually means the TREE moved after the build (the addon's source stamp covers
   native/, zcore/ and zdsp/ — tests included): rebuild the addon.
12. `node tests/e2e/mac/transport-race-e2e.cjs` — presses Play the way a singer does, which item 11 by construction never does: EARLY, before the graph prepared ahead has finished, and TWICE in a row. It also opens a SECOND song after the first, which is the sequence the field report of 2026-09-07 actually came from — item 11 was green throughout it. (a) Opening a song after another one cost TWO graph builds: the loader resets a dozen controls on the way in and each re-arms the prepare-ahead timer, which then fired while `engine.tracks` still held the song being LEFT, so a whole graph was built for the wrong song (the log says so — the wrong lane count), 2.3 s of blocked main, discarded unused; in the field the two songs differed in length and the core refused it outright as a start position outside the timeline. PASS requires exactly ONE `preparing graph` line per Play, on the first song AND across the switch. The switch leg is timing-sensitive and its default pair is not arbitrary: the window is the incoming song's decode with the outgoing lanes still installed, so E2E_SONG_B must be a LONG song — Mein Teil → Deutschland reproduces on the broken code, Wild World → Mein Teil does not. (b) The core's resume() refuses a transport that is already playing, and status is polled at 5 Hz, so a second Play inside one poll asked anyway and the refusal threw — leaving the button on Play for the rest of the song while the core played it. It therefore reads `__test.playing`, the BUTTON's own React state, and the core's `transportState`, and requires all three opinions to agree; every desktop driver before it read only `engine.playing`, which was right all along while the button was wrong (the phones learned this first). (c) A SELECTION the singer is not looping, made through the APP's own selection state (`__test.setSelection`) so every Play below goes through `togglePlay` exactly as the button and Space do. It forces the count-in off and refuses to run unless the engine accepted exactly a NON-looping selection, because a song saved with a loop or a count-in would pass it on the broken engine (a loop is never policed by the watcher; a count-in restart armed it all along). PASS requires the first Play from inside it to stop at its end, the next Play to play it again from its start and stop at its end, a third to do the same, a SEEK PAST the selection's end while it plays to land and keep playing (three times, at different phases of the watcher's 25 ms tick), and — with the selection cleared — a Play to run on past where it used to end; a Play that times out still playing is a failure. The seek check is the one that caught the fix's own side effect: once the watcher was armed on every Play, native `seek()` moved `startOffset` two IPC round trips after the position moved, and a watcher tick in between saw the old start beside the new position and paused the song where a lyric-line click or → had put it. It reproduced on the Windows field laptop on the first attempt with that half of the fix removed; `seek()` now moves `startOffset` when the seek is issued. Then OVERLAPPING seeks, issued back to back in one evaluate (a held arrow key repeats faster than a seek's two round trips), both playing from inside the selection — `togglePlay` moves a Play from anywhere outside a selection to its start, so nothing plays from past its end — one pair inside-then-past-the-end and one past-the-end-then-inside, each fired FOUR times at different phases of the watcher's tick: every attempt must land and have the bar moving on 1.5 s later. Moving the start at issue time broke those both ways — the first seek's receipt wrote its older target over the second's, and a seek queued behind another moved the start before the client's position moved at all — so the watcher does not judge while any seek is in flight, and seeks are numbered so only the latest touches the start when it settles. This check is a PROBABILISTIC detector, even on the field laptop: against the broken engine there, one run with a single pair per direction caught both, the next caught neither, and with four repeats a run caught the forward case on its fourth attempt — which is why it repeats. The Mac cannot catch it at all (its round trips are too quick for the race), so a green run of this check on the Mac alone says nothing. The deterministic guard is `tests/unit/engine-seek-race.test.ts`, which runs the real engine against a native client that seeks the way the desktop one does, at 3 to 60 ms a round trip and several watcher phases: 48 cases, all clean on the fix, 28 failing on the engine that only moved the start at issue time, 20 failing on main's engine where the selection did not hold at all, 13 failing with the in-flight gate alone removed (all of them backward), none with the numbering alone removed, and 28 with both removed: either one holds the forward pairs, only the gate holds the backward ones. The field report ("the first few times it plays just the selection, then the cursor leaves it and plays on") was the bound watcher: it was armed by the fresh-start path and by a count-in restart but NOT by the plain resume every later Play takes, nor by the adopt-a-running-transport return, so a selection bounded a song's first Play and nothing after it. `togglePlay` always seeked back to the selection's start; nothing stopped it at the end the second time. Do not set the engine's region directly in this leg: that skips `togglePlay`'s seek and tests a route no singer takes. The leg parks the transport before it starts, because the leg before it leaves the song playing and the first press would otherwise be a pause. The whole run also fails on ANY dsp warn or error in the log. Opens a project from the singer's own library read-only and restores its project.json in a `finally`. Refuses to pass vacuously: no native graph, no run.
13. `node tests/e2e/mac/lyrics-edit-e2e.cjs` — the lyrics editor end to end on a real project: the help sheet ("?" opens it, Escape closes the SHEET and never the editor — requestClose peels topmost-first), the keyboard layer (⌘E opens the word strip for the focused row with focus landing on its FIRST word chip — asserted with a budget, because the editor focuses it one frame after the strip mounts and a read straight after the mount is a coin flip — Escape peels the strip, ⌘Backspace removes the focused line), edit a line's text, ⌘Enter-stamp it at the playhead, "Align to the singing" (the editor's `lyrics:align-draft` path — a DIFFERENT code path from the panel's Check & align, which item 1 covers; the verdict string is generated from verdict data, so the fabricated marker line makes it read "1 line couldn't be made out and kept estimated timing" — an "every line snapped" here would itself be a regression), expand a row's voiceprint into the word strip and DRAG one word (it picks the word with the widest gap so the ±50ms neighbour fence can't eat the movement; asserts the move took and word order held), Save, then leave the song and reopen. PASS requires the persisted lyrics.json to say `source: 'edited'` with the marker text, monotonic line starts and word spans on every line, the panel badge flipping to `.src-badge.edited`, and the reopen serving the edit from cache (an 'edited' doc is sticky — never re-asked of LRCLIB, never superseded by a transcription). Backs up lyrics.json on entry and restores it in a `finally` AFTER the app closes — if interrupted, the project named in the output is the place to check.
13b. `node tests/e2e/mac/open-native-e2e.cjs` — the open does NOT decode under native playback: the lanes are
   MEASURED off their files by the addon (`lanes measured · 6 lanes · N ms` in main's log, none refused),
   no lane has samples in the renderer afterwards (`engine.getTrackBuffer(id) === null` for every stem,
   `lanesResident === false`), every lane still has peaks at one bucket per millisecond and a 96-bucket
   envelope, Play runs on the native graph and advances, the log stays free of dsp warnings, and the
   measured vocals agree with `computePeaks` over a decode fetched back through `ensureTrackBuffer`
   (≥97% of buckets within 0.05 — resampling ripple, the device's rate against the file's). Refuses to
   pass vacuously: no measure line, no run (native off, or no addon for this tree — build it first). It
   waits for the addon's warm-up line before clicking, as a singer's pause would, and prints the open's
   click → ready (0.56 s here for a 323 s six-lane song, against 2.7 s when the open decoded) and where
   the read's time went. Opens a library project read-only and restores its project.json in a `finally`.
13c. `node tests/e2e/mac/count-in-e2e.cjs` — the only desktop driver that turns the COUNT-IN on, and the guard for the field reports ("count-in is broken", "the seek bar jumps to the song start when I hit pause fast", "playback could not start: native metronome playback requires a beat grid") that every driver above was blind to because none of them counts in. Twelve legs on a library song with a beat grid: (1) scrub to E2E_MID before the first Play, press Play — a pre-roll must be seen, `engine.position` (what the bar, lyrics and pitch strip all draw) must HOLD at the landing through it (never dip below, never run ahead), the dots (`engine.countInStatus`, the transport's own value — they never showed under native before) must fill (see the dot rule below), and the song must come in at the landing with button, engine and core agreeing; (2) Pause INSIDE the count-in — the core must be paused at a negative frame (vacuity guard) and the bar must sit where it was, not at 0; (3) Play after that counts in AGAIN to the same landing; (4) Pause well into the song, Play counts in from THERE; (5) a CONTROL CHANGE inside the count-in — the click turned on while the core is still counting — must be a rebuild anchored at the landing (a pre-roll again, the bar held, the song at the chosen spot; the seam used to hand the old pre-roll clock to an unanchored plan and the song came in at the TOP), exactly three `preparing graph` lines for the Play, the touch and the touch-back; (6) the same touch while PAUSED inside the count-in — the bar must stay where it was (the rebuild used to carry the negative frame as its signed start and moved it to 0) and the Play after it must count in to the same spot, exactly four builds; (7) Play TWICE inside one restart — one build only, the three opinions agreeing whichever way the toggle landed (a second press used to stop the generation the first had just started and build another, so the count-in began twice); (8) count-in off, Play then Pause 200 ms later as a control — no pre-roll, bar within a poll of the press; (9) Space HAMMERED — bursts of real key events (six at 90 ms with the count-in off; three at 250 ms then five at 100 ms with it on) with the bar sampled at 30 ms, the only cadence that sees a one-poll dip: the core's audible projection is unavailable for a latency after every transport edge and its field defaults to 0, which the facade read as a position until 2026-09-15 ("still jumps if I press Space multiple times fast" — one poll at 0.00 on every playing→paused edge, count-in off or on); PASS requires no backward move of the bar over 0.3 s, nothing below the leg's floor, and the three opinions agreeing after each burst; (10) NO BEAT GRID with the metronome ON — `setBeats(null)` and `setMetronome({click:true})` must both be accepted, Play must start and count in gridless (three ticks an output second apart, the engine's grid checked null at the press so the leg cannot pass on a grid) with the bar held at the landing, `engine.playbackError` must stay null, and the click must come back the moment the grid does (cue events for the song's beats again). A click track does need a grid and all three bridges refuse one without, but the click is a saved setting of the SONG while the grid is a detection that can be absent, stale or still running, and the facade used to raise the mismatch as a product error — so that song could not be played at all, while Web Audio played it with its clicks silent. (11) a count-in that ends EXACTLY on a callback boundary — the driver learns the route's callback size from the core's `continuousFrame` steps (480 frames on WASAPI, 512 on the Mac; a route whose callbacks vary is SKIPPED out loud), aims a spot whose pre-roll is a multiple of it, and reads `desktopPlaybackStatus()` back-to-back from the page across the landing: PASS requires reads within a callback of the landing (vacuity guard) and none saying playing below it — the core used to publish playing at frame 0 for that one callback, which drew the bar at 0.00 one Space burst in five on the field laptop (every count-in there is aligned) and never on the Mac by luck; (12) the last click 20 and 35 ms before the landing — PASS requires all four dots (see the dot rule). THE DOT RULE: the dots must fill to total; one short is accepted only when the last dot was lit for less than trace's page recorder can see (`lastDotWindowMs` under 10 ms — the recorder records `engine.countInStatus` every 2 ms, about every 4 ms in practice). The last dot is lit from its click to the landing, a whole beat or a few milliseconds depending on where the singer paused. This rule used to forgive one short under 200 ms, reading a 3/4 there as a dot the sampling could not see; it was not lit at all — the dots read the render head unprojected, so a last click within one 50 ms status poll of the landing never lit (measured: 20 ms before the landing 3/4 in 2 of 2, 35 ms in 1 of 2, legs 4 and 6 red on the Mac whenever a Pause parked 19-39 ms past a beat). The dots project between polls now, so a 3/4 above the recorder's resolution is a product red, never the sampler's. Every count-in after the first is a restart (`restartWithCountIn`: stop, unload, prepare anchored at the paused spot, open, start), so PASS requires exactly two `preparing graph` lines for legs 3+4 — zero means a resume pretended to count in, more means a discarded build. Any dsp warn/error fails the run. Sets the metronome VOLUME to 0 as well as the master (the clicks bypass the master bus; main clamps the cue volume under SINGZ_MUTE now, this driver asks for 0 anyway). Opens a project from the singer's own library read-only and restores its project.json in a `finally`. Refuses to pass vacuously: no native graph, no planned count-in events, no run.
13d. `node tests/e2e/mac/space-focus-e2e.cjs` — Space is play/pause whatever the mouse touched last. Every button on the song screen keeps focus after a click, and from 2026-08-28 to the fix every focused button owned Space, so Space after Mute pressed Mute again and the song never started (and with a fader focused Space did nothing at all). Every driver above that presses Space does it with nothing focused, which is how that shipped green. Three legs with REAL mouse clicks and REAL key events (a DOM `.click()` does not move focus the way a singer's click does, and focus is the whole question): (1) click a lane's Mute, Space twice — the song must start and stop, `aria-pressed` must stay as the click left it, and Mute must not still hold focus; (2) click a stem fader — ArrowRight must move the fader and NOT seek the song (a focused slider owns its arrows, which is the half of the 2026-08-28 rule worth keeping), then Space twice must play and pause without touching it; (3) click into the bpm field — Space must NOT start the song (a field being typed into owns every key). A click that leaves focus somewhere else exits 2 INCONCLUSIVE, never PASS. Against the unfixed code it FAILS six ways on the Mac. Restores the song's project.json in a `finally`. On the field laptop use `E2E_PROJECTS_ROOT=<ps-lib> E2E_SONG="Player Session E2E"`; its clicks land late there (hidden-window stability waits), which costs time, not correctness.
13e. `node tests/e2e/mac/lyrics-cancel-e2e.cjs` — the singer starts "Check & align" over lyrics that are already on screen and changes their mind. PASS requires the panel to be EXACTLY what it was before the job: the same line texts (not merely as many — a panel that re-derived its words would restore a different 36 lines and pass a count check), the same `.src-badge` class and the same credit, reached without reopening the song. A cancellation was being applied as though it were a verdict on the lyrics, so the panel went to `idle`: 36 lines became 0 and stayed 0 until the song was reopened. Nothing on disk is wrong when this breaks — lyrics.json is never touched by a cancel — which is why no cache test could see it and why the state machine itself is pinned separately by `tests/unit/lyrics-state.test.ts` (11 cases; four mutations of the fix fail 4, 4, 2 and 2 of them). It also fails on any `warn`-level `align:` line in the log for the cancelled run: killing the aligner by signal used to log its non-zero exit and then its chunk's unplaced words as two warnings, so a run that stopped cleanly read as a broken one. That check only fires when the cancel reached the WORD ALIGNER, which lives past 60% of the job — the default leg stops the job wherever it has got to (a singer changing their mind, under a minute) and the run SAYS SO rather than letting the silence read as coverage: `align-warning check COVERS NOTHING this run`. `E2E_CANCEL_AT=align` waits for 60% first and makes it load-bearing; that costs the whole listen (~6 min cold on the field laptop, seconds when the listen cache hits), so run it when the aligner or the cancel path itself has changed. The scratch copy is made with `preserveTimestamps` for that reason — the listen cache is keyed on the vocals' size AND mtime, and a copy with fresh mtimes re-listens from cold every time, which pinned the cancel to the warm-up phase and made the check structurally unable to fire. Screenshots never gate the verdict (on a weak iGPU the emptied panel outran playwright's default and the red arrived as a TimeoutError with no verdict line). Needs the Qwen3-ASR speech model installed — without it main answers `needsModel` in milliseconds and the panel reaches the consent prompt before a Cancel button ever exists, so the driver exits 2 INCONCLUSIVE rather than passing vacuously (same for an align that answers before Cancel appears). Works on a scratch COPY opened through the hidden file input, so it has nothing of the singer's to put back.
13f. `node tests/e2e/mac/stems-after-end-e2e.cjs` — the stems come back after the song ran out. The field report (0.23.3, a 48 kHz WASAPI laptop) was "the metronome plays, but the stems don't": every lane is STREAMED, playing out of a window the core's feeder keeps ~2.7 s ahead and rendering silence for any frame it does not hold, and a lane the feeder had read to its END returned before the feeder looked at where playback wanted it next — so Play after the song ran out (count-in off: a seek of the SAME graph to the top and a resume) and a scrub back out of the last seconds were never chased, and every stem played silence while the metronome, which is no lane, clicked on. A RESAMPLED lane is the one that gets there (its decoder reaches the end of the file before the filter's flushed tail fills the ring), i.e. every 44.1 kHz stem on a 48 kHz output. A muted run cannot hear any of this — the transport, the button and the bar all move normally — so it reads each lane's `starvedBlocks` from the status (`streamed`/`starvedBlocks`, desktop-only lane keys) over 3 s of playing that starts a second after each jump, allowance 5 per lane. (1) Play the last 6 s out, the app parks at the end, Play again: the song restarts from the top on the SAME generation and its lanes are fed. (2) On a FRESH graph (a count-in Play builds one, so this leg cannot inherit leg 1's), play into the last ~2.4 s and seek back while playing. Each leg first proves the lanes were fed BEFORE its jump, so a red belongs to the jump. The whole run also fails on any dsp warn/error — main writes `lanes starving · generation N · …` once a streamed lane has starved without a break for 3 s. Mutation-checked on both platforms: with the early return put back, every lane starves every block in both legs — Mac (Mein Teil, 5 lanes 44.1k into 48k) 281-282 of ~282 per 3 s window; Windows field laptop (WASAPI, 480-frame callbacks, `Player Session E2E` 6 lanes 44.1k into 48k) 301 of ~300, twice — with a `lanes starving` line for each leg's generation; fixed, 0 on every lane on both, twice on the laptop. When you put a mutated source back for the fixed half of such a check, TOUCH it: a copy restored with its old timestamp is older than the control build's object file, the incremental build skips it, and the addon then carries the control code under the fixed tree's fingerprint (the fingerprint hashes sources, not the binary) — caught on the laptop. The run also builds one short-lived graph when it turns the count-in on while paused before leg 2; that is the ordinary metronome rebuild, replaced by the count-in Play. Refuses to pass vacuously: no native graph, an unstreamed lane or an addon without the counters is an error; a Play that built a fresh graph instead of resuming, or an output at the stems' own rate (nothing resampled), exits 2 INCONCLUSIVE — its first line prints the output rate and every lane's file rate. Count-in off and the metronome on (volume 0), as the report had it; pins the stream-lanes preference on for the run and puts it back. Restores the song's project.json in a `finally`. On the field laptop use `E2E_PROJECTS_ROOT=<ps-lib> E2E_SONG="Player Session E2E"` (the legs want a song of at least 40 s). The feeder itself is pinned by case 3c of `tests/native/streaming_lane_feeder_tests.cpp`, red on the old code for the resampled lane.

13g. `node tests/e2e/mac/played-edge-e2e.cjs` — the lanes' brightness step must sit ON the playhead line while a song plays. Field report 2026-09-24: "stem lines shadow removal is following the crosshair with delay" — the played layer had been put on a 4 Hz clock (a re-clip damages its whole visible part, and per pixel that was most of the Windows field laptop's GPU), and in a zoomed view the step trailed the line by up to ~64 CSS px while every driver stayed green. Since @singz/ui 1.9.0 each lane has `.wave-edge` (the played layer again, shown from the stack's `--p` to its own `--p-edge`); the loop writes `--p-edge` every step and catches `--p` up only once the sliver is 64 device px wide and at most every 250 ms. On a scratch copy of a library song (default "Deutschland"; E2E_SONG / E2E_PROJECTS_ROOT), hidden window: (1) whole-song view, 6 s playing — every lane's `--p-edge` equals the line's `--p` on EVERY frame, the played clip is never ahead of the line, the sliver stays within max(64 px, 250 ms of travel), big re-clips at most one per 64 px the line travelled + 2 (the 4 Hz clock made ~24 in those 6 s); (2) Pause lands the played clip exactly on the line; (3) zoomed eight steps (~3% of the song on screen, ~215 px/s here), 4 s — the same per-frame rules and at most 4 re-clips a second while the view holds (a view change remaps every percentage, those frames snap freely); (4) paused, capturePage at the machine's own dpr: eight consecutive line columns × two sliver starts, all snapped to whole WINDOW pixels the way the loop snaps them, must each render pixel-identical to the same line with no sliver (worst ≤ 2/255; the compositor rounds clip ends OUTWARD, so an end a float hair past its pixel boundary claims one more column — two layers meeting inside a pixel both paint it, a one-pixel bright seam, and the edge's own end paints one played column past the line — and a 4-decimal percentage lands either side depending on the column, so one line position is a coin flip), and the old lagging edge must differ (≥ 16/255) so the leg can see an edge at all. Mutation-checked: main's build (no edge layer) FAILs five rules (edge off the line on every frame, 24 big re-clips in 6 s, the seam at 67/255); the kit without its start nudge FAILs the seam leg at 34-36/255, without its END nudge at 11-65/255 on every column that rounds up (the first kit build shipped to the lanes that way; the mac lane caught it). On the Windows field laptop a hidden window paints about once a second, which starves legs 1-3: run it there with `E2E_VISIBLE=1` (shown without focus, maximized; it also asserts the page reports `visible`) plus the laptop's usual SINGZ_USERDATA_DIR / SINGZ_PACK_DIR and a lab COPY of a staged library as E2E_PROJECTS_ROOT. Verified there on the final build: every rule PASS, seam worst 2/255. Exit 2 when the song is missing.

14. `npm run capture:verify` — loads the worktree-local content-addressed addon inside real Electron, verifies its current source/manifest/raw SHA identities (plus canonical Mach-O identity on Mac), and checks its exports plus `inputDevices`/`captureState`/`captureStats` result shapes. An explicit package snapshot can be passed to the underlying driver with `--current-source`; only a signed packaged Mac artifact uses `--signed-packaged-mac`, where raw SHA may change but both strict codesign and the sealed signature-invariant canonical digest must pass. The signed-package E2E also changes a compiled byte and re-signs it; PASS requires rejection before require. Opens no audio device, so it is safe in automated runs; PR CI load-smokes arm64 Mac, x64 Mac and x64 Windows on matching CPUs, packages Windows, and transforms/re-signs a Mac package. Build the addon first (`npm run capture:addon`).
15. `node tests/e2e/capture-addon-hardware.cjs` — BY-HAND ONLY: opens the machine's real default microphone (needs TCC), streams live analysis windows and prints callback-to-JS latency percentiles. It captures the room, so it is never part of an automated pass — run it deliberately when verifying capture changes on real hardware, and say so in the report.
16. Read every screenshot the drivers report (Read tool) and confirm it shows what the text claims. A blank window is a failure.
17. Every driver launches with `SINGZ_E2E_HIDDEN: '1'` AND `SINGZ_NO_SYNC: '1'` in its env, and calls `quiet-launch.cjs` right after launch — the window must NEVER appear over the singer's work, and a driver's writes must never sync to the real Drive. The env is the real mechanism (main never shows the window and disables throttling so timers run full-rate hidden); the helper is only the fallback for builds predating the env, because its showInactive both races ready-to-show and, even winning, still raises a window over the work. On the sync side, the env var alone was NOT enough until main gated the dirty→scheduler wiring on it too: a save inside a driver marks the project dirty, and the scheduler would have pushed it four seconds later with the launch-sync gate green — code-certain, closed before it was ever observed, and verified by an align run that appended zero sync-log rows. A new driver copies a current driver's launch block, not an older one.
18. Flake note: the first wizard click can race the engine probe (driver retries internally); a selector timeout on `.lib-card` usually means a stale app instance survived — pkill and rerun.

## Platform: simulators (ios + android)

Do iOS first, then Android. All mobile commands run from /Users/maxplanck/Dev/my/SingZ/mobile.

Environment traps (do not re-learn):
- `pod install` needs `LANG=en_US.UTF-8`; move ios/build-device aside if pod chokes scanning it.
- NEVER pipe long-running commands through `head` (SIGPIPE kills Metro/emulator); never pipe xcodebuild through `tail` — redirect to a log file, check `$?`.
- Metro runs from mobile/ on 8081 (`packager-status:running` at /status); keep an existing one.
- Metro /json mixes ALL clients and keeps stale targets: filter deviceName (gphone=emulator, iPhone=sim) AND probe candidates with a 1+1 eval.
- Hermes CDP can segfault the app under eval churn — a "fresh app state" failure may be this; relaunch + rerun once.
- iOS sim UDID C624B667-6F58-4F85-B64F-63B75545DDE2, bundle io.s-dev.singz.
- Android: JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home (NOT the
  Android Studio JBR, which is JDK 25 now: AGP treats the restricted-native-access warning from
  its `prefab` subprocess as a build error, and this app has prefab consumers — reanimated,
  worklets, and Oboe. Never derive it from `java_home -v 21`, which exits 0 handing back a JDK 8
  on this Mac), SDK ~/Library/Android/sdk, AVD "SingZ_API36", always `adb -s <serial>`; debug builds load JS from Metro at 10.0.2.2:8081.

iOS:
1. `node scripts/patch-audio-api.js` (idempotent — all patches must report applied/present).
2. `cd ios && LANG=en_US.UTF-8 pod install > /tmp/pod.log 2>&1; echo EXIT=$?`
3. Boot sim, then `xcodebuild -workspace ios/SingZPlayer.xcworkspace -scheme SingZPlayer -configuration Debug -destination "id=<UDID>" -derivedDataPath ios/build-sim build > /tmp/ios.log 2>&1; echo EXIT=$?`
4. `xcrun simctl install <UDID> ios/build-sim/Build/Products/Debug-iphonesimulator/SingZPlayer.app`
5. From the repo root, run the permanent tests sequentially: `node mobile/tests/loop-region.cjs`, `node mobile/tests/ab-repeat.cjs`, `node mobile/tests/seek-memory.cjs`, `node mobile/tests/open-close-memory.cjs`, `node mobile/tests/song-sheet-beat.cjs`, `PLATFORM=ios node mobile/tests/move-to-drive.cjs` — each prints PASS. `ab-repeat` is loop-region's other half: loop-region drives `engine.setRegion` (the mechanism, never in doubt), `ab-repeat` drives `cycleLoop`, the callback the A–B control's `onPress` is wired to (RN has no DOM, so no CDP driver reaches an actual tap) — the three-state cycle and the marks the scrub rail's band is drawn from, both of which shipped in v0.18.0 with `TEST.cycleLoop`/`TEST.loopMarks` exported and nothing referencing either. It also proves the loop goes round, asserting the position lands near A rather than merely "inside the region", which everything including the start point satisfies. Unlike its siblings it resolves the device NAME from `SIM_UDID` and matches Metro targets on that, so it still picks the right device with two simulators up (or a real phone on the same Metro, which is how the gap was found). `song-sheet-beat` seeds its own two projects in Documents and takes ~90 s; it is the only one that reads a SCREEN rather than the engine (the Song sheet's Beat row, watched through an analysis). `move-to-drive` (Phase 6) serves a fake Drive over HTTP on DRIVE_PORT (8799), rewrites the generated `mobile/src/gdrive-config.ts` to aim the app at it and restores it on exit — run it with nothing else on that Metro — seeds two songs, moves them through the library's own "Add all local songs to Google Drive" path and the app's natives, then runs the REAL desktop sync against the same store; it needs `SIM_UDID` (or `PLATFORM=android ANDROID_SERIAL=<emulator>`, which needs `adb root`). Run it three times, once per variant: default, `STAY_ON_PHONE=1`, and `CUT_MID_BATCH=1` (the fake Drive drops every request from the second song's first write — the song that went up must be listed and open with no signal, across a cold restart — then comes back for the next "Add all"). Each also seeds a phone copy of the desktop's song (never offered, marked "Also in Google Drive") and removes it on the way out.
6. `node mobile/tests/player-session.cjs --platform ios` — the backend comparison, and the slowest thing here (~15 min: it runs the whole session TWICE, once on legacy and once on the native graph, and prints them side by side). Run it after any playback change, and before a release that ships the experimental toggle. Preconditions it will refuse to start without: the Mac's default output at 48 kHz (below that the simulator's RemoteIO refuses the native handoff and the "native" pass measures legacy while claiming otherwise). One rule while it runs: **edit nothing under `mobile/`** — Metro reloads the app's JS on any change there and unmounts the player mid-session; the suite catches that with a run id and stops, but the pass is lost. Metrics, the pass rule and the two metrics deliberately NOT compared (the transpose, a full graph rebuild under native, held to an absolute ceiling; and first-audible, which the two backends measure from different events) are in `mobile/tests/player-session/README.md`. It is listed under iOS only because that is where it is usually started; **the same numbered step belongs to the Android leg too** — the command with `--platform android` drives an emulator, with the stale-APK and never-speak-during-a-decode rules enforced in the driver rather than remembered. `--platform ios-device` drives a REAL iPhone and is deliberately never part of a bare run: it is somebody's own handset and the run writes into its library, so it must be asked for by name (`IOS_DEVICE=<name|udid|identifier>`) and refuses outright when two devices are attached. It needs a development- or ad-hoc-signed Debug build and a phone that can reach this Mac on the Metro port — there is no `adb reverse` for iOS. Its CPU and memory columns come back BLANK: there is no `top` for a process on a phone, and a host-side number would describe the Mac. Two more things about it since the native clock and the background hold landed: it refuses to start on a busy Mac when a simulator or emulator is in the run (`--wait-quiet` blocks until three quiet load samples; `ALLOW_BUSY_HOST=1` runs anyway and fails the host-quiet rule, so the CPU columns never pass by accident), and on the Android leg its background step no longer holds the stream: since Now Playing (0.22.0) a song left PLAYING is kept playing under the media session (NowPlayingService), and since the sample-background fix a song left playing keeps playing on EITHER backend there, so the `backgrounded` CPU phase is COMPARED now rather than carved out — it was legacy's suspended context that made it UNCOMPARED; `graph released=0 preparing graph=0` across the window still holds. The stream HOLD itself is exercised by `now-playing.cjs` (a pause from the media controls behind Home, and a song that runs out there) and by `focus-loss-android.cjs` window 3. What no driver covers yet, and a phone check should: a foreground flip that lands mid-park, a metronome preview click straight after coming back (a held stream would swallow it), and audio-focus loss while parked.

Android:
1. Boot the AVD if `adb devices` is empty (nohup the emulator binary; wait for sys.boot_completed).
   Then silence it: `adb -s <serial> shell cmd media_session volume --stream 3 --set 0`
   (verified on API 36; the old `media volume` command no longer exists).
2. `cd mobile/android && ./gradlew installDebug > /tmp/droid.log 2>&1; echo EXIT=$?` — in a fresh
   worktree there is no gitignored `local.properties`; pass `ANDROID_HOME=~/Library/Android/sdk`
   in the environment instead of writing one.
3. `adb -s <serial> shell am start -n com.lexasoft.singz/com.singzplayer.MainActivity` (the code
   package differs from the applicationId — `/.MainActivity` does not resolve), wait ~10s.
4. Drive over CDP (pattern: read mobile/tests/loop-region.cjs for the ws/eval plumbing, but launch via adb, filter gphone): `__test.openSample()` → poll player+duration → `play()` → 3s → `position > 2` → `pause()` → `__test.back()` → poll catalog → `openSample()` again and confirm it loads (release-on-close path).
   Then `PLATFORM=android ANDROID_SERIAL=<serial> METRO_PORT=<port> node mobile/tests/move-to-drive.cjs` — owed on Android specifically: it is the one platform whose `moveProjectToCache` COPIES between storage areas (external files → filesDir are separate mounts) where iOS only renames, and whose upload runs under the stall watchdog.
5. Capture needs the SIDE-BY-SIDE debug app, which steps 2-4 do not install: build and install
   it first — `cd mobile/android && ./gradlew assembleDebug -PdebugAppIdSuffix=.debug > /tmp/mic.log 2>&1; echo EXIT=$?`
   (redirect, never pipe: a piped gradle reports TAIL's status and a build that died reads as
   exit 0, after which step 2's unsuffixed APK is what gets installed and the driver blames a
   missing package), then
   `adb -s <serial> install -r app/build/outputs/apk/debug/app-debug.apk`, launch
   `com.lexasoft.singz.debug/com.singzplayer.MainActivity`, and `adb reverse tcp:<METRO_PORT>
   tcp:<METRO_PORT>`. Then `ANDROID_PKG=com.lexasoft.singz.debug node mobile/tests/mic-android.cjs` — vocal-training
   CAPTURE, which nothing else here covers. It asserts the transport contract, not pitch: the
   input inventory and that Bluetooth never outranks the built-in microphone, the negotiated
   device/lane/format/rate, that the preset was READ BACK rather than merely requested, that
   audio actually arrives and the hardware callback fired with no overruns, and that the lease
   releases cleanly enough for a second capture to succeed. Level and pitch thresholds are
   deliberately NOT asserted — that needs a controlled acoustic environment and is pinned
   instead by the host suite's mutation-checked fixtures (tests/native/core_host_tests.cpp).
   It drives `__test.audioInput` directly, never the training screen, because tapping fixed
   coordinates tests a layout and every fault this covers was in the transport.
   Then, on the same build and package, run `focus-loss-android.cjs` (below) — the two share
   a build, and the focus one is worthless against a package the driver cannot reach.
- `node mobile/tests/play-from-anywhere.cjs --platform ios` (and `--platform android` on an
   emulator) — the native backend's Plays that are not "Play from the top of a fresh song",
   which the player-session harness never drives because it seeks, loops and pauses only
   AFTER Play, and which reached a phone in build 49 with nothing covering them: a scrub
   before Play (count-in off → flat start at the target; on → a pre-roll of negative frames
   with the bar sweeping the beats before the target, the dots lit on the clock, a landing on
   the target), an A-B armed before Play (loops inside [A,B)), Play after a pause with the
   count-in on (the graph stops and an anchored prepare counts in again with NOTHING
   DECODED — decoded lanes are parked and adopted, streamed lanes, the default since
   bdaf4e8c, are released and streamed again, and the leg asks the restarted generation's
   `laneDecodeFallback`; the landing is the paused spot) and off (a plain resume, no
   prepare), and the seek bar's level envelope
   compared across BOTH backends on one song (the colour agrees per sliver, the levels agree
   where there is signal). Silent throughout; ~2 min; prints PASS.
- `node mobile/tests/now-playing.cjs --platform ios` (and `--platform android` on an emulator,
   after ANY change to NowPlaying.m / NowPlayingModule.kt / NowPlayingService.kt /
   src/playback/now-playing.ts, or to how the app backgrounds) — the song on the Lock Screen,
   in Control Center and in Android's media session and notification, read back from the OS
   itself (iOS `NowPlaying.debugState()` reads MPNowPlayingInfoCenter/MPRemoteCommandCenter;
   Android adds `dumpsys media_session` and `dumpsys activity services`), never from what the
   app meant to send. Legs: shown on open (paused, title, no next/previous track), Play
   (playing at the tempo; Android's mediaPlayback foreground service up), pause / scrub /
   skip / play from OUTSIDE the app (Android play and pause are REAL media keys via
   `cmd media_session dispatch`; iOS goes through the DEBUG `debugCommand`, which enters the
   same handler the Lock Screen's targets call — the Simulator cannot press one), HOME while
   playing (the song must keep advancing; on Android the background park must NOT happen and
   the log must say the media session holds it), pause and play from outside while
   backgrounded (Android parks and HOLDS the stream on that pause), then leaving the song
   clears the card (Android: session inactive, service gone). This is the guard for App
   Review's 2.5.4 rejection of 0.19.0 — `audio` background mode with nothing visible using
   it. Needs the DEBUG build of THIS tree. Silent throughout; ~2 min; prints PASS.
- `node mobile/tests/sample-background.cjs` (and `--platform android`) — the bundled
  sample behind the Home Screen, on whatever backend a FRESH INSTALL uses (legacy: the
  sample decodes bundled assets). `now-playing.cjs` forces the native backend and so
  cannot see this path; App Review rejected 0.22.0 and 0.19.0 under 2.5.4 because of it.
  Run it after anything that touches backgrounding, the engines, or the sample.
- `node mobile/tests/waveform-streamed.cjs` (and `--platform android` with
   `ANDROID_PKG=com.lexasoft.singz.debug` on a device or emulator) — the seek bar's waveform
   on a STREAMED song, and the only driver whose song is deliberately LONG. The envelope is
   measured by a background pass that fills the lanes in one at a time, and `lanePeaks()`
   used to cache whichever answer it got first: the screen's first ask lands 400 ms after it
   mounts, with nothing measured, so the bar polled a frozen "0 of 6" for seventy-two seconds
   on a song whose pass had finished in four. Every suite stayed green through four broken
   builds because the bundled sample is 40.8 s — six lanes of it measure in ~0.25 s, so the
   race was always already won. This one loops the sample (LOOPS=16, ~11 min) so the pass is
   still working when the screen mounts, samples the SAME object the seek bar asks while the
   open is still running, and asserts the answer IMPROVED rather than merely arrived. A run
   where the pass beat the first ask exits 2 as INCONCLUSIVE rather than passing — lower
   LOOPS and it stops being able to fail, which is the whole failure mode it exists for.
   Verified to fail against the old code on a POCO X6 Pro (frozen at 3 of 6 for 117 s).
   ~2 min per platform; prints PASS.
- `ANDROID_PKG=com.lexasoft.singz.debug node
   mobile/tests/focus-loss-android.cjs` — audio focus loss under NATIVE playback, the one
   thing the player-session comparison cannot take away from the app: a plain playing
   song, an ARMED SWAP (a metronome change has claimed a candidate generation and the seam
   has not landed — the core keeps the song playing when only the candidate is cancelled,
   so the bridge must retire both), a HELD stream (paused, then backgrounded, the graph parked —
   paused first because a song left playing is kept playing under the media session since
   0.22.0), and a song PLAYING under that media session behind Home (window 4: the loss must
   stop it, the log must say so, and Play afterwards must work). It
   asserts that the song stops and stays stopped, that the log says why, and that Play
   afterwards starts fresh — window 3 is where the facade used to answer "Android audio
   focus is not owned" until the held-rate poll read the core. The loss is delivered by
   `NativeAudioRuntime.debugAudioFocusChange`, which exists only in DEBUG builds and hands
   AUDIOFOCUS_LOSS to the same listener on the same handler AudioManager uses; Android
   delivering the callback is Android's contract, everything from the listener down is
   ours. A release build has no such method and the driver says so. Fifteen rules; a full pass
   is 15/15.
   NEEDS REAL AUDIO INPUT: an emulator booted `-no-audio` has none and the driver will
   correctly, uselessly, report a capture that delivers nothing — prefer an attached phone, and
   note this is the one step here that cannot run on a silent AVD.

v0.14.1-test6 — Test build: one door left, and it was locked from inside

test5 found your RTX 3060, introduced the new engine to it — and then the
splitter's command-line parser rejected the engine's name one step before
the music started, because its list of allowed engines predates this one.
This build hands the parser a name it accepts and wires the new engine in
underneath; the log will say "TensorRT RTX session created" the moment it
actually holds the GPU. Also in this build: the old DirectML engine is gone
for good — it never completed a split on any machine we have ever seen.

To test it: install (no new pack needed), switch to GPU in the model
manager, split a new song, send the log. Same as always — and thank you for
the sixth round tonight.

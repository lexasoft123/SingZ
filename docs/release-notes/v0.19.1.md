v0.19.1 — The Mac app just opens now, and Re-detect keeps your bar lines

<!-- store:en-US -->
Bar lines you corrected on the computer now survive its Re-detect button. It used to drop them and sync the loss down here, so a song you had already fixed came back wrong on the phone. The phone's own Detect again has always kept them; now both do. Nothing else on the phone changes in this one.
<!-- /store -->

<!-- store:ru-RU -->
Тактовые черты, поправленные на компьютере, теперь переживают его кнопку «Определить заново». Раньше она их теряла и синхронизировала потерю сюда — песня, которую вы уже поправили, приходила на телефон без них. Собственное «Определить заново» на телефоне их всегда сохраняло; теперь так делают оба. Больше в этом обновлении на телефоне ничего не меняется.
<!-- /store -->

Mac:

- **It opens.** Every SingZ before this one was met by macOS with *"Apple
  could not verify…"*, and the way past it was a trip into System Settings,
  or a line typed into Terminal that nobody should have to be told. Apple has
  checked this build, and that whole ritual is gone: download, drag it to
  Applications, open it. Same on a friend's Mac you send it to.

Mac and Windows:

- **Re-detect no longer throws away your bar lines.** If you had nudged a
  couple of bars into the right place on a song and then pressed Re-detect in
  the Metronome window, they went, quietly, and the next save carried the loss
  to Google Drive and on to your phones. It was not the detector disagreeing
  with you — the button simply built the new grid without them. (0.18.0's
  notes warned about this and promised it separately; this is that.)

  Your corrections come back onto the fresh grid now, re-snapped to the beats
  the detector just found. That is what the automatic re-reading has always
  done and what your phone has always done, and one button had no business
  meaning two different things.

  The Metronome window says so, too: beside the grid version it now counts
  what is yours — *· 2 hand-set bars*, in the same green those lines are drawn
  in on the waveform — and the button itself promises to keep them. If you
  want a genuinely clean grid, remove the lines you placed and then press it.

- **A smaller download.** The installer had been quietly carrying things it
  never needed — around 14 MB of it on each Mac build, 10 MB on Windows.
  Nothing is missing; there is simply less to fetch and less sitting on your
  disk.

iPhone:

- **There is an iPhone app now**, playing your synced library with the same
  lyrics, melody line and beat grid as the Android one. It is in open beta
  while Apple reviews the store listing — join at
  [testflight.apple.com/join/q6vVdkwt](https://testflight.apple.com/join/q6vVdkwt).

Nothing here changes how a song sounds.

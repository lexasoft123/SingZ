<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Requested only from the singer's explicit vocal-training action. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <!-- Keeps a PLAYING song alive once the app leaves the screen (NowPlayingService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:enableOnBackInvokedCallback="false"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!-- Split jobs run in their own process: the ORT session alone is
           ~1.3 GB RSS, and a memory kill there must never take the player.
           Type ladder at runtime: mediaProcessing on 35+, dataSync 29..34,
           typeless below. -->
      <service
        android:name=".split.SplitService"
        android:process=":split"
        android:exported="false"
        android:foregroundServiceType="dataSync|mediaProcessing" />
      <!-- The media notification for a playing song, and the foreground service
           that keeps it playing behind the lock screen. Same process as the
           player on purpose: it talks to NowPlayingModule directly. Foreground
           only while a song plays; see NowPlayingService. -->
      <service
        android:name=".NowPlayingService"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>

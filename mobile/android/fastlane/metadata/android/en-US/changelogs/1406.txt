A proper icon, and one audio engine instead of two.

The launcher icon is now an adaptive one, so it takes the shape your launcher uses instead of sitting in a white square. SingZ also needs Android 8 now rather than Android 7: below 8 the sound went through an entirely different audio backend that we had no way to test, and shipping an audio app on an untested audio path is not support, it is a guess.

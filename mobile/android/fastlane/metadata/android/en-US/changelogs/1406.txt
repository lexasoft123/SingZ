A proper icon, and one audio engine instead of two.

The launcher icon is adaptive now, so it takes the shape your launcher uses instead of sitting in a white square. Signing in to Google Drive is sturdier too: the one-time code is protected with SHA-256 rather than sent in the clear. SingZ needs Android 8 now — below it, sound went through a different audio backend we had no way to test.
